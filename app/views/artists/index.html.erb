<h1>Artists</h1>

<ul>
    <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %></li>
    <li><%= a.song_count > 1 ? "#{a.song_count} songs" : "#{a.song_count} song" %></li>
    <% end %>
</ul>
