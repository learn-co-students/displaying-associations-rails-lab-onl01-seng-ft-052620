<h3>Artists</h3>

<ul>
<% @artists.each do |a| %>
    <li>
        <%= link_to a.name, artist_path(a) %>
        <p>
        <%= pluralize(a.song_count, "songs") %>
        </p>
    </li>
<% end %>
</ul>